<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #3498db; color: white; padding: 20px; text-align: center; }
    .content { padding: 20px; background-color: #f9f9f9; }
    .job-card { background-color: white; padding: 15px; margin: 10px 0; border-left: 4px solid #3498db; }
    .footer { text-align: center; padding: 20px; color: #777; font-size: 12px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>New Jobs Match Your Search!</h1>
    </div>
    <div class="content">
      <p>Hello {{ userName }},</p>
      <p>We found <strong>{{ matchCount }} new job(s)</strong> that match your saved search: <strong>{{ searchName }}</strong></p>
      
      {{#each jobs}}
      <div class="job-card">
        <h3>{{ this.jobTitle }}</h3>
        <p><strong>Location:</strong> {{ this.workLocation }}</p>
        <p><strong>Pay:</strong> ${{ this.amount }}</p>
        <p><strong>Job ID:</strong> {{ this.jobId }}</p>
      </div>
      {{/each}}

      <p>Log in to your account to view all matches and apply!</p>
    </div>
    <div class="footer">
      <p>&copy; 2024 WorkNow. All rights reserved.</p>
    </div>
  </div>
</body>
</html>

